query GetUser($username: String!) {
	getUser(username: $username) {
		user {
			id
			username
			email
			avatarUrl
			coverPhotoUrl
			name
			location
			website
			birthday
			bio
			createdAt
		}
	}
}
