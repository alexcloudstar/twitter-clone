import React, { FC } from 'react';
import { ProfileWrapper } from './style';
import { ProfileProps } from './types';

const Profile: FC<ProfileProps> = () => {
	return <ProfileWrapper>Profile page</ProfileWrapper>;
};

export default Profile;
